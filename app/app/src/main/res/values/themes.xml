<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal system theme — all UI is Compose-driven.
         Parent must exist on API 26+ (our minSdk).
         Material3 colors are set in MainActivity.kt DarkColorScheme. -->
    <style name="Theme.RGBController" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#FF0F0F14</item>
        <item name="android:navigationBarColor">#FF0F0F14</item>
        <item name="android:windowBackground">#FF0F0F14</item>
    </style>
</resources>
